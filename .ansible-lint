# warn_list:
#   - experimental
skip_list:
  - no-changed-when
  - literal-compare

exclude_paths:
  - .venv/
  - .env
  - .github/
  - terraform/
  - "*.md"

# CI mode - skip syntax-check that requires vault decryption
offline: true
